
1. Register_Endpoint :

 Request :
    First_name ,
    last_name  = while giving response to the user it should (firstname + lastname)
    username - mandatory - str
    email  - mandatory - str -Also it should check ending @gmail.com else raise Error.
    phone_number - mandatory - int 
    password - it should store in an encryption format. - str 
    DOB, 
    DOJ,
    Address. - str
    it will provide by the user .
    Internally in database it should maintain 
    one feild  status : Active and Inactive ( if user exceed maximum attempts  for 3 times we mark this status as inactive for 24hrs. )
    * timestamp (name based meaning fullconventation ) - for password creation maintain timestamp - After everyonemonth user need to change his password(change_password is another Endpoint)
    Else it should not allow login (we will define this logic in login Endpoint)
    Do not allow duplicate email,phone_number,username .

 Response :
   return username successfully registred with email id.

2. login end_point:
   Request :
      username or email
      password 
   
   Note : for password from date of created it should valid only upto onemonth if it exceed more than month it should not allow for 
   login and return  please change your password and try again login.
   
   Response :
      login successfull message with username
      jwt token - expriy time 1 hr.

3. Update_Details end_point :
   Request:
      token - provided by login Endpoint
      password

      username
      first_name
      last_name
      email
      phone_number
      dob
      address

      conditions :
      1) Recogize the user through token and password (allow update only if the token user matches the target user) who ever wanted to update their login details 
      2) If same details given for update it should not update details and return some message
      3) Not all fields are mandatory.If nothing was provided then raise details not provided.
      
      
   Response:
      details updated successfully with the username.

4. change Point end_point : (Defect found)
   
   Request :
    username
    email
    password

    Note : Previous used passwords cannot be used futher.
    while when changing passowrd make sure to update the timestamp also in database which was created internally in Register_Endpoint.
    logic for password change: while user registration internally maintain one list for password when ever user try to change_password compare with that list and then accept the change.


   Response :
    password changed successfully with  some message.